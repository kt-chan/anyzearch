name: anyzearch

networks:
  anyzearch:
    driver: bridge

services:
  anyzearch:
    container_name: anyzearch
    build:
      context: ../.
      dockerfile: ./docker/Dockerfile
      args:
        ARG_UID: ${UID:-1000}
        ARG_GID: ${GID:-1000}
    cap_add:
      - SYS_ADMIN
    volumes:
      - "./.env:/app/server/.env"
      - "../server/storage:/app/server/storage"
      - "../collector/hotdir/:/app/collector/hotdir"
      - "../collector/outputs/:/app/collector/outputs"
      - "../certs/anyzearch.local.crt:/temp/certs/anyzearch.local.crt"
      - "../certs/anyzearch.local.key:/temp/certs/anyzearch.local.key"
    user: "${UID:-1000}:${GID:-1000}"
    ports:
      - "3001:3001"
    env_file:
      - .env
    networks:
      - anyzearch
    extra_hosts:
      - "host.docker.internal:host-gateway"
